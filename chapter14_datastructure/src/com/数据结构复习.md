## 数据结构

数据结构的研究对象：

* 数据间的逻辑关系（几何关系、一对一、一对多、多对多）
* 数据的存储结构（或物理结构）
    * 角度一：顺序结构、链式结构、索引结构、哈希结构
    * 角度二：线性表（一维数组、链表、栈、队列）、树（二叉树、B+树）、图（多对多）、哈希表（HashMap、HashSet）
* 相关运算

## List 接口下的实现类的源码剖析

### 1. 面试题：ArrayList，LinkedList，Vector 的三者区别？

* `ArrayList`：`List` 的主要实现类：线程不安全的、效率高；底层使用的是`Object[]` 数组存储
    * 在添加数据，查找数据时，效率较高；在插入、删除数据时，效率较低。
* `LinkedList`：底层使用双向链表的方式进行存储
    * 在添加数据，查找数据时，效率较低；在插入、删除数据时，效率较高。在对集合中的数据进行频繁的删除、插入操作时，建议使用此类。
* `Vector`：`List` 的古老实现类（已经很少使用）：线程安全的、效率低；底层使用的是`Object[]` 数组存储。

## Map 接口下的实现类的源码剖析

### 1. HashMap 的底层源码剖析（重点）

### 2. LinkedHashMap 的底层源码剖析

### 3. HashSet，LinkedHashSet 的底层源码剖析

## 面试题

### 数据结构相关

1. 链表和数组的区别
2. 栈、队列的区别

### List 集合源码相关

#### 1. ArrayList 的默认大小是多少，和扩容机制。

默认大小为 10，扩容为原先的1.5倍。

#### 2. ArrayList 的底层是怎么实现的？

#### 3. 在 ArrayList 中调用 remove 后后面几个元素该怎么做？

#### 4. ArrayList 1.7 和 1.8 的区别

类似于饿汉式、懒汉式

#### 5. 数组和 ArrayList 的区别

ArrayList 可以看作是对数组的常见操作的封装。

#### 6. 什么是线程安全的 List？

Vector：线程安全的。
ArrayList：线程不安全。可以使用同步机制处理。

```text
额外的：
HashMap：线程不安全。
    1. 使用同步机制处理。
    2. JUC：ConcurrentHashMap
```

### HashMap 集合源码相关

#### 1. HashMap 的底层实现原理。

#### 2. HashMap 初始值为16，临界值12是怎么算的。

临界值 `threshold`，使用`数组的长度 * 加载因子(loadFactor：0.75)`

#### 3. HashMap 长度为什么是2的幂次方

方便计算要添加的元素的底层的索引 i。

#### 4. HashMap 怎么计算哈希值和索引？扩容机制？怎么解决哈希冲突？

#### 5. HashMap 为什么长度达到一定的长度要转化为红黑树？

#### 6. HashMap 什么时候扩充为红黑树，什么时候退化为链表？

#### 7. jdk1.7和1.8中 HashMap 的区别

#### 8. HashMap 的 get() 方法的原理

#### 9. hashCode() 和 equals() 的区别？

#### 10. hashCode() 与 equals() 手写方法的注意事项？

进行 equals() 判断使用的属性，通常也都会参与到 hashCode() 的计算中。尽量保证 hashCode() 的一致性。

##### 11. 说一下 equals 和 == 的区别？equals 相等hash 值一定相等吗？hash值相等equals一定相等吗？

* equals 相等 hash 值一定相等吗？是
* hash 值相等，equals 一定相等吗？不一定

### Set 集合源码相关

#### 1. hashSet 存放数据的方式？

底层使用 HashMap。

#### 2. Set 是如何实现元素的唯一性？

#### 3. 用哪种方式来实现集合的排序

自然排序、定制排序。